# satflux.io Standalone Deployment Configuration
# Save this as deploy.config.sh on the server to use these settings

COMPOSE_FILE="docker-compose.standalone.yml"
PROJECT_NAME="satflux_standalone"

# To use this with the deploy script, you can symlink it:
# ln -sf deploy.config.standalone.sh deploy.config.sh
